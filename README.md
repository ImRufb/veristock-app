   git init
   git add index.html
   git commit -m "Inicializar app de inventario"
   git branch -M main
   git remote add origin https://github.com/TU_USUARIO/inventario-web.git
   git push -u origin main
